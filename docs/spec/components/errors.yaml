components:
  schemas:
    InvalidArgumentError:
      type: object
      properties:
        message:
          type: string
          example: Invalid email
    NotFoundError:
      type: object
      properties:
        message:
          type: string
          example: Something was not found

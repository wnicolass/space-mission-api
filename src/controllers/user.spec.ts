import { describe, expect, test } from 'vitest';

describe('User Controller', () => {
  describe('Test POST /users', () => {
    test('It should respond with a 200 ok', () => {
      expect(200).toBe(200);
    });
  });
});
